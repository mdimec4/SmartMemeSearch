<Window x:Class="SmartMemeSearch.Wpf.Views.FirstRunDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome to Smart Meme Search"
        Background="#F3F3F3"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Segoe UI Variable"
        Padding="20">

    <Window.Resources>

        <!-- Base Windows 11 colors -->
        <SolidColorBrush x:Key="Win11Background" Color="#F3F3F3"/>
        <SolidColorBrush x:Key="Win11Card" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Win11Border" Color="#D0D0D0"/>
        <SolidColorBrush x:Key="Win11Button" Color="#E9E9E9"/>
        <SolidColorBrush x:Key="Win11ButtonHover" Color="#DDDDDD"/>
        <SolidColorBrush x:Key="Win11Text" Color="#202020"/>

        <!-- Modern Win11 Button Style -->
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI Variable"/>
            <Setter Property="Foreground" Value="{StaticResource Win11Text}"/>
            <Setter Property="Background" Value="{StaticResource Win11Button}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Win11Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">

                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="root" Property="Background"
                                        Value="{StaticResource Win11ButtonHover}"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="root" Property="Background"
                                        Value="#CFCFCF"/>
                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>


    <!-- CONTENT AREA -->
    <Border Background="{StaticResource Win11Card}"
            BorderBrush="{StaticResource Win11Border}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="20">

        <StackPanel Width="400">

            <TextBlock Text="Set up your meme folders"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="{StaticResource Win11Text}"/>

            <TextBlock Text="Before using Smart Meme Search, choose one or more folders where your memes are stored."
                       Margin="0,12,0,20"
                       FontSize="14"
                       TextWrapping="Wrap"
                       Foreground="{StaticResource Win11Text}" />

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Skip" Width="80" Click="Skip_Click"/>
                <Button Content="Choose Folders" Width="140" Click="Choose_Click"/>
            </StackPanel>

        </StackPanel>

    </Border>

</Window>
